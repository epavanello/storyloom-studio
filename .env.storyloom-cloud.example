# Inference profile: every generative capability uses OpenRouter.
# Copy to `.env.storyloom-cloud`, then run `pnpm dev:cloud` or `pnpm worker:cloud`.
# Database, Redis, R2/S3, auth secrets and any operator key belong in `.env`.

STORYLOOM_MODE=cloud
STORYLOOM_WORKER_CONCURRENCY=2

OPENROUTER_LLM_MODEL=deepseek/deepseek-v4-flash-0731
OPENROUTER_TTS_MODEL=google/gemini-3.1-flash-tts-preview
OPENROUTER_IMAGE_MODEL=google/gemini-3.1-flash-image

TEXT_POLICY=cloud-only
TTS_POLICY=cloud-only
IMAGE_POLICY=cloud-only
ALIGNMENT_POLICY=cloud-only
