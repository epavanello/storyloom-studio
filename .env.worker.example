# Detached worker: the web app is deployed somewhere cheap, this machine owns the
# models. It attaches to the same Postgres, Redis and object storage, and drains
# only its own account's private queue.
#
#   pnpm worker            # after `set -a && . ./.env.worker && set +a`
#   node --env-file=.env.worker node_modules/.bin/tsx src/worker.ts
#
# Find the exact queue selector for your account under Settings.

STORYLOOM_MODE=local

DATABASE_URL=postgres://user:password@ep-xxxx-pooler.eu-central-1.aws.neon.tech/storyloom?sslmode=require
REDIS_URL=rediss://default:password@your-redis-host:6379
# Must match the value used by the web deployment.
STORYLOOM_QUEUE_PREFIX=storyloom
STORYLOOM_ENCRYPTION_KEY=

# The same bucket the web app writes to, so a render made here is readable there.
STORAGE_DRIVER=s3
S3_BUCKET=storyloom-artifacts
S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
S3_REGION=auto
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# Only this account's queue. Never add `cloud` here: this machine's runtimes would
# then be used to serve other accounts.
STORYLOOM_WORKER_QUEUES=local:REPLACE_WITH_YOUR_USER_ID
STORYLOOM_WORKER_CONCURRENCY=1
# A local chapter render can hold a job for a long time.
STORYLOOM_WORKER_LOCK_MS=600000

LOCAL_LLM_BASE_URL=http://127.0.0.1:1234/v1
LOCAL_LLM_MODEL=storyloom-text
LOCAL_LLM_MODEL_KEY=qwen/qwen3.6-35b-a3b
LOCAL_TTS_BASE_URL=http://127.0.0.1:7861/v1
LOCAL_TTS_MODEL=tts-1-it
LOCAL_TTS_RUNTIME_MODEL=mlx-community/Qwen3-TTS-12Hz-1.7B-CustomVoice-8bit
LOCAL_IMAGE_BASE_URL=http://127.0.0.1:7862/v1
LOCAL_IMAGE_MODEL=black-forest-labs/FLUX.2-klein-4B
LOCAL_IMAGE_RUNTIME_MODEL=mlx-community/flux2-klein-4b-4bit
LOCAL_ALIGNER_BASE_URL=http://127.0.0.1:7863/v1

TEXT_POLICY=local-required
TTS_POLICY=local-required
IMAGE_POLICY=local-required
ALIGNMENT_POLICY=local-required
